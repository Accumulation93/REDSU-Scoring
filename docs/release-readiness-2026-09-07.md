# 签名复验与统一选择卡片交付记录

## 本次范围

- 最终 PDF 保存前自动复验；签名域正确落在指定页；增加独立 OpenSSL 诊断及多轮多步回归。
- 验签结果突出完整姓名、签署时组织和岗位，技术检查及 Adobe 信任说明保留在独立报告。
- 修复证书导出单一路径：手机分享、电脑保存互为备用，另提供同一公开证书 PEM 文本复制；重写报告说明。未核实平台身份的外来签名不显示平台托管说明、经办人或证书信任入口。
- 一并交付此前已完成的整卡科技蓝选择、移除姓氏装饰、人员选择器层级及印章外卡布局；保留现有滚动实现，不继续处理已由用户撤销的滚动问题。
- `output/`、`outputs/` 和用户原始 PDF 不修改、不纳入提交。

## 已完成门禁

- 159 个服务端测试文件通过，包括独立 MySQL 的凭证、文件事务回滚、迁移、重提交及身份关联测试；测试库与生产隔离，临时数据库进程已关闭。
- 新增连续三轮、每轮八步、多页每步多处图层及最终纯通过回归；每轮文件通过 OpenSSL 和平台凭证链校验，历史字节未变。故意破坏生成结果时保存前失败关闭。
- 增加另造私钥并冒用相同证书主体、任意自签 PDF 上传、真实清单移植换钥重签回归；签名有效与平台来源认可分开验证。证书分享/保存备用、取消、同步异常和 PEM 原字节还原均有组件回归。
- 28 个前端回归、修改 JS 语法、严格 UI、微信兼容、模板运行时、组织生命周期、安全、敏感字面量、租户 SQL、时间、本地化、权限审计及 `git diff --check` 通过；生产依赖审计无漏洞。
- 用户停止电脑测试之前，微信开发者工具编译主包与全部六个分包成功；后续证书导出、文案与来源展示调整只做自动化检查，未再次启动开发者工具编译。生产迁移预检待执行为零，无数据库结构变更。

## 现场核对与边界

- 原始 PDF 在 Adobe 显示“签名有效性未知”，明确说明文档未被修改、证书尚未受信任；同一原文件通过独立 OpenSSL，未修改原文件及任何信任设置。
- 生产服务只读完整验签通过：文档、CMS、两步凭证、身份和平台登记证书均通过；外部公共信任未确认。
- 已在登录后的管理端实际选取并上传原文件，结果为验证通过；Pad 竖屏两步记录均显示同一报告内签署人编号，姓名、组织和岗位层级正常、最后一步与报告入口可达。独立报告及公开证书导出入口可见。
- 用户明确要求“停止电脑测试，直接部署”，此后停止全部电脑操作。手机、Pad 横屏、普通用户端及实际证书分享未完成现场检查，不宣称已验收；相关自动化检查不替代这些现场项目。
- 第三方默认信任取决于收件人阅读器的信任配置或认可 CA，不能承诺未知证书永远不出现 Adobe 顶部警告。

## 发布方式

按项目流程中文提交、推送 `main`，由 GitHub Actions 质量门禁通过后部署；部署完成还须核对完整提交、进程、迁移及内外健康，失败停止后续发布。微信正式版本不自动提交。
